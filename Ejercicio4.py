def listAverage(list):
    """Función que recibe una muestra de números en una lista y devuelve su media.
    Parámetro: list = lista de números introducida por el usuario.
    Salida: Número que devuelve la media de la lista intoducida."""
    return sum(list)/len(list)